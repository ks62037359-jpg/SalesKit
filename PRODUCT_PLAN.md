# SalesKit AI 기획서

## 한 줄 정의

영업 사원이 타깃 업체 URL과 제안 서비스를 입력하면, AI가 업체를 분석하고 선택한 템플릿에 맞춰 HTML 콜드메일과 후속메일 초안을 생성하는 구독형 B2B 영업 생산성 도구.

## 초기 타깃

- B2B 영업 사원
- 1인 세일즈 창업자
- 홈페이지 제작사, SaaS, HR, 영상 제작, 컨설팅 업체의 신규 영업 담당자
- 반복적으로 콜드메일을 쓰지만 매번 업체 분석과 문장 작성에 시간을 쓰는 사람

## 핵심 문제

- 타깃 업체를 조사하고 제안 논리를 만드는 데 시간이 오래 걸림
- 콜드메일이 복붙처럼 보여 답장률이 낮음
- 후속메일, 콜드콜 오프닝, 제안서 초안까지 매번 새로 만들기 귀찮음
- 영업 사원 개인은 비싼 CRM이나 복잡한 세일즈 솔루션을 쓰기 부담스러움

## MVP 가치 제안

> 업체 URL만 넣으면 3분 안에 맞춤형 콜드메일 초안을 만든다.

초기 버전은 실제 AI 크롤링 전에도 아래 흐름을 검증할 수 있어야 한다.

1. URL 입력
2. 내가 파는 서비스 입력
3. 수신자, 톤, CTA 선택
4. 템플릿 선택
5. 타이틀, 목차, 섹션 본문, 명함 구조의 HTML 메일 미리보기 생성
6. 텍스트 복사, 메일 본문 형식 복사, 초안 저장
7. 우측 초안 직접 수정 및 AI 수정 요청 반영

## 기본 메일 구성

사용자가 만든 기존 제안 메일의 흐름을 기준으로 아래 구조를 기본값으로 둔다.

1. 타이틀
2. 인사 및 발신 배경
3. 목차
4. 01 발신 배경
5. 02 진단 결과
6. 03 핵심 솔루션
7. 04 기대 효과
8. 05 미팅 제안
9. 하단 명함

이 구조는 단순 콜드메일보다 제안서형 메일에 가깝다. 영업 사원이 상대 업체에 대해 사전 분석을 했다는 인상을 주는 것이 핵심이다.

## 초안 편집 방식

우측 생성 결과는 사용자가 직접 수정할 수 있어야 한다.

- 직접 수정 모드: 생성된 HTML 메일 본문을 화면에서 바로 수정
- AI 수정 요청: “더 정중하게”, “더 짧게”, “CTA를 강하게”, “진단 결과를 부드럽게” 같은 요청을 입력해 초안 변경
- 복사와 저장은 원본 생성물이 아니라 사용자가 수정한 현재 본문을 기준으로 처리

실제 AI API 연결 전 MVP에서는 자주 쓰는 수정 요청을 규칙 기반으로 먼저 지원하고, 이후 OpenAI API로 교체한다.

현재 구현은 OpenAI API가 설정되어 있으면 서버의 `/api/generate-mail`, `/api/edit-mail`을 통해 AI 생성/수정을 사용하고, API 키가 없거나 호출이 실패하면 로컬 규칙 기반 생성으로 fallback된다.

API 키는 브라우저 코드에 넣지 않고 서버 환경변수 `OPENAI_API_KEY`로만 읽는다.

## 리서치 반영 개선 사항

GitHub 리서치 팩 기준으로 MVP에 다음 항목을 반영한다.

- 영업 단계 선택: 첫 접촉, 후속 연락, 미팅 확정, 제안서 전달, 재접촉
- 수신자 관점 선택: 의사결정권자, 실무자, 대표/임원, 구매 담당자
- 변수 미리보기: `{{company}}`, `{{recipient}}`, `{{industry}}`, `{{offer}}`, `{{cta}}`, `{{sender_name}}`, `{{sender_phone}}`
- 금지 표현 감지: 성과 보장, 무조건, 최저가, 100% 개선, 반드시 1위
- 저장 초안 목록: 최근 저장한 초안을 우측 패널에서 다시 불러오기

## HTML 메일 호환 기준

주요 메일 플랫폼의 작성창에 붙여넣어 쓰는 것이 핵심 사용 방식이다.

우선 지원 대상으로 보는 플랫폼:

- Gmail
- 네이버메일
- 다음메일
- Outlook 웹메일

HTML 생성 규칙:

- 외부 CSS 사용 금지
- `<style>` 태그 의존 금지
- class 기반 스타일 의존 금지
- JavaScript 사용 금지
- 복잡한 flex/grid 사용 금지
- table layout과 inline style 중심으로 생성
- 이미지 없이도 설득 가능한 구조 우선
- 버튼은 실제 `<button>`이 아니라 스타일이 적용된 텍스트/링크 형태로 표현
- 복사 시 가능하면 `text/html`과 `text/plain`을 함께 클립보드에 저장
- `text/html` 복사가 막히는 브라우저에서는 숨겨진 리치 본문을 선택 복사하는 방식으로 대체
- 최후의 fallback에서도 HTML 코드 원문이 아니라 텍스트 본문만 복사

주의점:

메일 플랫폼별 보안 정책 때문에 일부 스타일은 제거될 수 있다. 따라서 디자인보다 가독성, 문단 구조, CTA 명확성을 우선한다.

## 구독 모델

| 플랜 | 가격 | 대상 | 제한 |
| --- | ---: | --- | --- |
| Starter | 월 19,000원 | 개인 영업 사원 | 월 100건 생성 |
| Pro | 월 49,000원 | 적극적으로 영업하는 실무자 | 월 500건, HTML 템플릿, 후속메일 |
| Team | 월 149,000원 | 소규모 영업팀 | 팀 템플릿, 공유 라이브러리, 통계 |

## 2주 MVP 범위

- 정적 프론트 MVP 제작
- 템플릿 5종 구성
- 입력값 기반 메일 생성
- HTML 미리보기
- 텍스트/HTML 복사
- 브라우저 로컬 초안 저장
- 서비스 가격/로드맵 화면

## 다음 개발 단계

1. OpenAI API 연결
2. URL 텍스트 추출 서버 추가
3. 회원가입과 생성량 제한
4. Stripe 또는 국내 PG 구독 결제
5. PPT 제안서 초안 생성
6. 템플릿 마켓 또는 팀 공유 기능

## 주의할 포지셔닝

마케팅 대행 도구가 아니라 B2B 영업 생산성 도구로 정의한다.

피해야 할 표현:

- 광고대행사 영업 자동화
- 마케팅 대행 제안서 자동 제작
- 네이버 광고 제안 메일 생성기

권장 표현:

- B2B 영업 사원을 위한 콜드메일 자동 생성 도구
- 업체 URL 기반 맞춤형 영업 메일 제작 툴
- HTML 콜드메일과 후속메일을 빠르게 만드는 세일즈 워크벤치
